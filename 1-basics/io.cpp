#include <iostream>

using namespace std;

int main(){
    // Write a program to read 3, space-seperated, integers from the standard input
    // and output their sum.

    int a, b, c;
    std::cin >> a >> b >> c;
    cout << a+b+c << endl;

    
    return 0;
}